// An empty Prisma Schema with no models, perfect as a starting point for your own schema.
// https://github.com/prisma/prisma-schema-examples/blob/main/empty/prisma/schema.prisma

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

